[project]
name = "plant-points-app"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "cryptography>=48.0.0",
    "kivy>=2.3.1",
    "pymysql>=1.1.3",
]

[project.scripts]
app = "main:main"

[tool.uv]
package = true

[tool.setuptools.packages.find]
where = ["src"]